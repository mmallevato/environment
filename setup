Get softwares
  Chrome
  Atom

Set up environment
  ./bashrc

Get & set up Anaconda
    Get Anaconda
    Set up Bioconda
      $ conda config --add channels r
      $ conda config --add channels defaults
      $ conda config --add channels conda-forge
      $ conda config --add channels bioconda
    Update Anaconda
      $ conda update --all
    Get libraries
      $ conda install java-jdk htslib fqtools samtools hisat2 freebayes bcftools snpeff rpy2 plotly biopython pyfaidx yapf pylama
      $ pip install pytabix fastqp

Get Jupyter VIM binding extension

Get Node.js

Setup Atom
  Get packages
    atom-beautify
    autocomplete-python
    color-picker
    ex-mode
    git-plus
    git-time-machine
    hydrogen
    linter-eslint
    linter-python
    python-tools
    vim-mode-plus
    react-snippets
