Get Ubuntu 16.04 LTS

  Update BIOS using Internet cable
  
  (Set UEFI)
    Legacy Mode = Off (default)
    Secure Boot = Off
    Graphics = Discrete
    Port security = Off (for the dock)

  Install Ubuntu 16.04 LTS

  Remove softwares

  Update softwares

  Trim launcher
  
  Hide launcher

  Increase typing speed

  Set file view (Files --> Edit --> Preferences)

  Remove nautilus shortcuts
    $ vi ~/.config/user-dirs.dirs

  Set vi as the default text editor
    $ sudo update-alternatives --config editor


Setup environment

  Get Chrome

  Get ~/.bashrc from ./bashrc

  Get softwares
    $ apt install screen tree gparted

  Get & setup Anaconda
    Get Anaconda
    Update Anaconda: $conda update --all
    Create Python2.7 environment: $conda create -n py2.7 python=2.7 anaconda && conda update --all -n py2.7
    Set up Bioconda
    Get programs: $conda install gcc java-jdk r htslib samtools bcftools hisat2 freebayes

  Get Python packages: $ pip install rpy2 plotly biopython pytabix GEOparse

  Get Jupyter VIM binding extension

  Get Node.js

  Get & setup Atom
    Get Atom
      $ sudo add-apt-repository ppa:webupd8team/atom
      $ sudo apt update; sudo apt install atom
    Get packages:
      atom-beautify
      autocomplete-python
      color-picker
      git-plus
      git-time-machine
      linter-eslint
      linter-python
      python-tools
      vim-mode-plus
      vim-mode-plus-ex-mode

  Get Caffein
  Get VPN: $ sudo apt-get install network-manager-vpnc network-manager-vpnc-gnome
  
  Finalize launcher
  
