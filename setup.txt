Get Ubuntu 16.04 LTS

  Update BIOS using Internet cable

  (Set UEFI)
    Legacy Mode = Off (default)
    Secure Boot = Off
    Graphics = Discrete
    Port security = Off (for the dock)

  Install Ubuntu 16.04 LTS

  Trim launcher

  Remove softwares

  Update softwares & install additional drivers ()

  Increase typing speed

  Set file view (Files --> Edit --> Preferences)

  Remove nautilus shortcuts
    $ vi ~/.config/user-dirs.dirs

  Set vi as the default text editor
    $ sudo update-alternatives --config editor


Setup environment

  Get Chrome

  Get ~/.bashrc from ./bashrc

  Get softwares
    $ sudo apt install screen tree exfat-utils exfat-fuse

  Get & setup Anaconda
    Get Anaconda
    Update Anaconda
      $ conda update --all
    Set up Bioconda
      $ conda config --add channels conda-forge
      $ conda config --add channels defaults
      $ conda config --add channels r
      $ conda config --add channels bioconda
    Get softwares
      $ conda install java-jdk r htslib samtools bcftools hisat2 freebayes snpeff rpy2 plotly biopython pyfaidx yapf
      $ pip install pylama pytabix

  Get Jupyter VIM binding extension

  Get Node.js

  Get & setup Atom
    Get Atom
      $ sudo add-apt-repository ppa:webupd8team/atom
      $ sudo apt update; sudo apt install atom
    Get packages
      atom-beautify
      autocomplete-python
      color-picker
      git-plus
      git-time-machine
      linter-eslint
      linter-python
      python-tools
      vim-mode-plus
      vim-mode-plus-ex-mode
      react

  Finalize and hide launcher
